{% extends 'base.html.twig' %}
{% block title %}twig!
{% endblock %}

{% block body %}
	<h1>Bonjour je suis
		{{class}}
	</h1>

	{% set pi=3.14 %}
	{{pi}}


	{% set tableau = [1, 2, 3] %}
	{% for elt in tableau %}
		<li>{{elt}}</li>

	{% endfor %}


	{% set tab = {'id': 'value1', 'name': 'value2'} %}
	{# {% for elt in tab %} #}
	<ul>
		<li>{{tab.id}}</li>
		<li>{{tab.name}}</li>
	</ul>

	{# {% endfor %} #}


	{% set tabL = [{'id': 'value1', 'name': 'value2'},{'id': 'value1', 'name': 'value2'}] %}
	{{tabL | length}}
	{% for elt in tabL %}
		<ul>
			<li>{{"l'id est:  " ~ elt.id ~ " le nom est:  " ~ elt.name | upper}}</li>
			{# <li>{{elt.name}}</li> #}
		</ul>

	{% endfor %}
{% endblock %}
